### Executive Summary
Полная версия сохранена в `audit.md`; ниже тот же отчёт в требуемом формате. Проект ― это full-stack инструмент Multi-LLM Analyzer: Flask backend (≈600 строк Python) принимает CSV/Excel/PDF, чистит данные pandas/pdfplumber и проксирует запросы в OpenAI/YandexGPT/GigaChat, а CRA/TypeScript фронтенд (≈2.4k строк) визуализирует таблицу, строит чарты (Recharts) и генерирует PDF через WeasyPrint. Основные рабочие файлы — `backend/pdf_server.py` (329 строк), `frontend/src/App.tsx` (477 строк) и монолитный `frontend/src/components/AnalysisResult.tsx` (1110 строк). Репозиторий несёт реальные CSV/XLSX бенчмарки и документацию (`docs/*`), но отсутствие конфигурационных слоёв приводит к дублированию `.env`, хардкоженным URL и ручной оркестрации.

Код работает частично: фронт/бэк стартуют локально, но backend падает без всех ключей GigaChat, npm test ломается на устаревшем “learn react”, а API ключи уже лежат в двух `.env`. Без аутентификации и rate limiting любой пользователь может загружать 100 МБ файлов и запускать LLM/WeasyPrint. Большинство функций объединяет загрузку, бизнес-логику и интеграции в одном слое, а каждый “load more” триггерит полный пересчёт и повторный LLM-запрос. Нулевая автоматизация (нет Docker/CI, тестов, зависимостей не пинят) делает перезапуск и деплой рискованными.

Структура проекта:
```text
project/
├── backend/
│   ├── pdf_server.py – Flask endpoints upload/analyze/report/fill (329 строк)
│   ├── llm/
│   │   ├── main_processor.py – роутер к OpenAI/Yandex/Giga
│   │   ├── openai_helper.py – клиент OpenAI SDK
│   │   ├── yandex_gpt_helper.py – REST клиент YandexGPT
│   │   └── gigachat_helper.py – адаптер gigachat SDK
│   └── requirements.txt – 13 непинованных зависимостей
├── frontend/
│   ├── src/
│   │   ├── App.tsx – главный контейнер (477 строк) + состояние очистки данных
│   │   ├── components/AnalysisResult.tsx – визуализация/экспорт/рекомендации (1110 строк)
│   │   ├── components/{FileUpload,ModelSelector,ManualFillDialog,AIFillDialog}.tsx
│   │   └── api/index.ts – axios-клиент с жёстким API_BASE_URL
│   └── package.json – 21 зависимость CRA/MUI/Recharts
├── docs/{API,ARCHITECTURE,DEPLOYMENT}.md – описание ручного запуска
├── README.md – продуктовый обзор на русском
├── .env и backend/.env – реальные LLM ключи
└── sample CSV/XLSX – тестовые датасеты
```

Ключевые метрики:

| Метрика | Backend | Frontend | Примечание |
|---------|---------|----------|------------|
| Строк кода | ~600 | ~2 400 | `rg` по *.py / *.ts(x) |
| Файлов | 15 | 28 | node_modules/build исключены |
| Функций/компонентов | 15 | 34 | AST/regex подсчёт |
| Средний размер функции | ~33 строк | ~72 строки | upload_file = 110 строк |
| Макс размер файла | 329 строк (`backend/pdf_server.py`) | 1110 строк (`frontend/src/components/AnalysisResult.tsx`) | |
| Type coverage | ~40 % (частичные аннотации) | ~60 % (TS strict, но много `any`) | |
| Test coverage | 0 % | 0 % (fixture learn-react) | |
| Lint warnings | Не настроено | CRA eslint не запускался | |
| Security issues | 3 critical (секреты, raw HTML, debug) | 1 high (хардкод API host) | |
| Дублирование кода | ~10 % (LLM helperы, мёртвые функции) | ~25 % (три шаблона PDF/HTML, дубли состояний) | |
| Зависимостей | 13 | 21 | pytesseract/python-docx не используются |
| Устаревших пакетов | 13 (версии не закреплены) | ≥5 (react-scripts 5.0.1, react-router-dom 7.6.3 не используется и т. д.) | |
| Цикломатическая сложность (avg) | ~8 | ~12 | оценка по размеру функций |

**Безопасность**

🔴 SECURITY ISSUE #1  
Файл: `.env:2-12` и `backend/.env:2-13`  
Тип: Хардкод API ключей (OpenAI, Yandex, GigaChat) в репозитории  
Код: `OPENAI_API_KEY=sk-proj-TryYbcrV...`, `YANDEX_API_KEY=AQVN1OxX...`  
Риск: ВЫСОКИЙ — реальные секреты уже попали в Git, любой клон получает полный доступ к провайдерам.

🔴 SECURITY ISSUE #2  
Файл: `backend/pdf_server.py:296-315`  
Тип: Принимается произвольный `report_html` и без очистки отправляется в WeasyPrint  
Код: `html = HTML(string=data['report_html'])` → `return send_file(BytesIO(pdf))`  
Риск: ВЫСОКИЙ — можно внедрить `<img src="file:///etc/passwd">` / SSRF или вложить скрипты в итоговый PDF.

🔴 SECURITY ISSUE #3  
Файл: `backend/pdf_server.py:357-358`  
Тип: Flask запускается `app.run(host='0.0.0.0', debug=True)`  
Риск: ВЫСОКИЙ — удалённый интерактивный дебаггер позволяет выполнить произвольный код при падении.

🔴 SECURITY ISSUE #4  
Файл: `backend/pdf_server.py:141-355`  
Тип: Весь API (upload/analyze/report/fill) открыт без аутентификации, CORS фиксирован только на localhost, rate limiting отсутствует, а DEBUG-логи содержат таблицы пользователей.  
Риск: ВЫСОКИЙ — неавторизованный DoS (100 МБ upload), утечка данных и злоупотребление платными LLM.

**Критические баги**

🐛 BUG #1  
Файл: `backend/llm/gigachat_helper.py:8-23`  
Тип: Исключение при импорте без переменной `GIGACHAT_CREDENTIALS`  
Код: `raise ValueError("GIGACHAT_CREDENTIALS не заданы")` в теле модуля  
Последствия: backend не стартует в production-режиме даже если GigaChat не используется.

🐛 BUG #2  
Файл: `frontend/src/App.tsx:213-244`  
Тип: Подгрузка страниц сбрасывает пагинацию и повторно анализирует весь массив  
Код: `setUploadResult({... current_page:1, total_pages:1})` + `analyzeLLM(table_data: newAllData)`  
Последствия: UI теряет информацию о страницах, а каждое “Load more” отправляет всю таблицу обратно, экспоненциально удорожая LLM-запросы.

🐛 BUG #3  
Файл: `frontend/src/components/AIFillDialog.tsx:25-33`  
Тип: `useState` инициализируется из `suggestions` и никогда не синхронизируется при смене столбца  
Код: `const [filled, setFilled] = useState(...suggestions...)` без `useEffect`  
Последствия: при переходе к следующему отсутствующему столбцу диалог показывает старые значения и отправляет неверные подсказки.

🐛 BUG #4  
Файл: `frontend/src/App.test.tsx:5-9`  
Тип: Тест ищет “learn react”, которого нет в приложении  
Последствия: `npm test` гарантированно падает и CI нельзя подключить.

**Архитектура**

⚠️ ARCHITECTURE ISSUE #1  
Файл: `backend/pdf_server.py:1-358`  
Проблема: Один модуль совмещает загрузку файлов, очистку данных, работу с LLM, генерацию PDF и конфигурацию — отсутствует разделение слоёв/сервисов.  
Нарушает: Single Responsibility, усложняет тестирование и переиспользование.

⚠️ ARCHITECTURE ISSUE #2  
Файл: `frontend/src/App.tsx:1-377`  
Проблема: 477‑строчный компонент управляет загрузкой файлов, очисткой пропусков, LLM-запросами, диалогами и пагинацией.  
Нарушает: SRP, отсутствует декомпозиция на контейнеры/хуки.

⚠️ ARCHITECTURE ISSUE #3  
Файл: `frontend/src/components/AnalysisResult.tsx:1-1110`  
Проблема: Компонент содержит бизнес-логику, анализ данных, построение графиков, формирование HTML/PDF, вызовы backend и state для экспорта.  
Нарушает: Разделение ответственностей, повторяет код (три функции `generate*PDF`) и мешает оптимизациям.

⚠️ ARCHITECTURE ISSUE #4  
Файл: `frontend/src/api/index.ts:3`, `frontend/src/App.tsx:286-295`, `frontend/src/components/AnalysisResult.tsx:666-710`  
Проблема: Жёстко прописанный `http://localhost:5000` как для axios, так и для fetch-запросов PDF/AI Fill.  
Нарушает: Конфигурацию окружений (dev/prod), блокирует деплой за пределы localhost.

**Производительность**

🐌 PERFORMANCE ISSUE #1  
Файл: `backend/pdf_server.py:173-214`  
Проблема: Любой CSV/Excel читается целиком через pandas перед нарезкой по страницам; `process_large_csv` не используется.  
Следствие: >100 МБ файлы целиком попадают в память и на диск, что даёт OOM/медленный отклик.

🐌 PERFORMANCE ISSUE #2  
Файл: `backend/pdf_server.py:333-350`  
Проблема: Алгоритм AI-fill для каждого пустого значения строит маску `mask & (df[c] == row[c])` по всему DataFrame и повторяет фильтрацию.  
Следствие: O(n²) при количестве строк > 1000, сильная нагрузка на CPU.

🐌 PERFORMANCE ISSUE #3  
Файл: `frontend/src/App.tsx:238-244`  
Проблема: Каждое обновление данных (load more, AI-fill, manual fill) заново отправляет полный массив строк в `/api/analyze`.  
Следствие: Размер тела запроса и время LLM пропорциональны количеству уже загруженных строк, UI зависает.

🐌 PERFORMANCE ISSUE #4  
Файл: `frontend/src/components/AnalysisResult.tsx:802-848`  
Проблема: `categoricalColumns`, `uniqueValues`, `stringStats` вычисляются с нуля при каждом рендере без `useMemo`.  
Следствие: Любое изменение стейта приводит к полной переработке десятков тысяч ячеек в JS-потоке.

**Code Quality**

📝 CODE QUALITY ISSUE #1  
Файл: `frontend/src/components/AnalysisResult.tsx:32-48`  
Проблема: Состояния объявлены как `any` (`useState<any>({})`, `const [filteredData, setFilteredData] = useState<any[]>(...)`).  
Сложность: теряются подсказки TS и легко внести баги при работе с таблицей.

📝 CODE QUALITY ISSUE #2  
Файл: `frontend/src/api/index.ts:8-60`  
Проблема: `validateUploadResponse` выводит в консоль полный ответ/таблицу (`console.log('Raw response data', response.data)`).  
Сложность: логируются потенциально чувствительные данные и захламляется консоль/сеть.

📝 CODE QUALITY ISSUE #3  
Файл: `backend/pdf_server.py:205-214`, `296-320`, `323-354`  
Проблема: В критичных местах использованы широкие `except Exception as e` с возвратом `str(e)` клиенту.  
Сложность: нет типизированной обработки ошибок, в ответ уходит внутренний текст исключения.

📝 CODE QUALITY ISSUE #4  
Файл: `backend/pdf_server.py:117-140`  
Проблема: `process_large_csv` не вызывается (мертвый код), хотя описывает логику для больших файлов.  
Сложность: разработчики могут думать, что оптимизация включена, но она не работает.

📝 CODE QUALITY ISSUE #5  
Файл: `frontend/src/App.tsx:286-295` и `frontend/src/components/AnalysisResult.tsx:693-716`  
Проблема: Вручную вызывают `fetch` вместо уже настроенного axios-клиента, дублируя обработку baseURL/ошибок.  
Сложность: повышает риск несогласованной конфигурации и отсутствия единых interceptors.

**Инфраструктура и конфигурация**

🔧 INFRASTRUCTURE ISSUE #1  
Проблема: `.env` и `backend/.env` с реальными ключами лежат в репозитории несмотря на `.gitignore`.  
Последствия: секреты уже скомпрометированы, dev/prod конфиги не разделены.

🔧 INFRASTRUCTURE ISSUE #2  
Проблема: Нет Docker/compose, CI/CD, процессов миграции — запуск описан только текстом в `docs/DEPLOYMENT.md`.  
Последствия: ручное развёртывание, высокая вероятность расхождений окружений.

🔧 INFRASTRUCTURE ISSUE #3  
Файл: `frontend/src/api/index.ts:3` и `frontend/src/App.tsx:286-295`  
Проблема: Жёсткий HTTP (не HTTPS) URL `http://localhost:5000`, отсутствуют переменные окружения для production.  
Последствия: невозможность безопасно опубликовать backend, так как фронт перепрошит на localhost.

🔧 INFRASTRUCTURE ISSUE #4  
Файл: `backend/pdf_server.py:12-20`  
Проблема: `logging.basicConfig(level=logging.DEBUG)` в проде плюс подробные `logger.debug` с колонками.  
Последствия: чувствительные данные попадают в логи и занимают диск.

🔧 INFRASTRUCTURE ISSUE #5  
Файл: `backend/pdf_server.py:189-207`  
Проблема: Временный файл сохраняется `temp_file_name = os.path.join(tempfile.gettempdir(), filename)`, но никогда не удаляется.  
Последствия: накопление временных файлов при множественных загрузках.

**Зависимости**

📦 DEPENDENCY ISSUE #1  
Файл: `backend/requirements.txt`  
Проблема: Все 13 зависимостей без версионных пинов; часть (pytesseract, python-docx) нигде не используется.  
Риск: reproducibility отсутствует, возможны неожиданные обновления и bloat в контейнере.

📦 DEPENDENCY ISSUE #2  
Файл: `frontend/package.json`  
Проблема: `react-router-dom@7.6.3`, `@mui/x-date-pickers`, `@date-io/date-fns` не используются в коде; `react-scripts@5.0.1` официально EOL.  
Риск: лишние мегабайты в bundle, уязвимости в неиспользуемых пакетах.

📦 DEPENDENCY ISSUE #3  
Файл: корень `package.json`  
Проблема: Дублирует часть зависимостей фронтенда (MUI, React) вне `frontend/`, что сбивает разработчиков и может конфликтовать с npm install в корне.  
Риск: несогласованные версии, сложнее автоматизировать сборку.

**Python-специфика**

🐍 PYTHON PATTERN ISSUE #1  
Файл: `backend/llm/yandex_gpt_helper.py:54-70`  
Проблема: `requests.post` вызывается без `timeout`, а при сетевых проблемах цикл ретраев блокирует весь поток Flask.  
Рекомендовано: добавить явные timeouts и class-based клиенты; сейчас любой подвисший запрос блокирует обработку.

**JS/TS-специфика**

⚛️ REACT PATTERN ISSUE #1  
Файл: `frontend/src/components/ModelTest.tsx:18-33`  
Проблема: `setTimeout` запускается для каждого элемента и не очищается при размонтировании, хотя компонент предназначен для тестирования моделей.  
Последствия: утечки таймеров и лишние dispatch’и при быстром переключении/уходе со страницы.

⚛️ REACT PATTERN ISSUE #2  
Файл: `frontend/src/App.tsx:300-307`  
Проблема: `useEffect(() => { if (aiFillOpen) handleAIFillStart(); }, [aiFillOpen])` подавляет ESLint зависимостьми (`// eslint-disable-next-line`) и игнорирует `handleAIFillStart` в deps.  
Последствия: сложно гарантировать корректность размонтирования, возможны гоночные условия при переопределении функции.

**Метрики и состояние**

- Функциональность: Работает частично (нужны реальные ключи, backend падает без GigaChat credentials, экспорт PDF и AI fill жёстко завязаны на localhost).
- Качество кода: 4/10; Читаемость 4/10; Структура 3/10; Документация 3/10 (фронтовый README всё ещё шаблон CRA); Тесты 1/10; Безопасность 2/10; Производительность 4/10.
- Общая оценка: 4/10.
- Технический долг: Критический (≈80 часов на ликвидацию); блокирует развитие из-за утечки секретов, отсутствия тестов и монолитных компонентов.

**Контекст для рефакторинга**

- Цель: LLM-анализ автомобильных датасетов (CSV/Excel/PDF), заполнение пропусков и выпуск отчёта.
- Текущее использование: Dev-окружение; нагрузка — до 1000 строк на страницу (README ограничивает 100 МБ).
- Ограничения: Обратная совместимость не критична (прототип), дедлайна нет, команда 1 человек, доступное время ~40 часов/итерация.

### Критичные находки (топ-5)
1. [SEC-1] Реальные API ключи лежат в `.env` и backend/.env → любой клон имеет доступ к OpenAI/Yandex/GigaChat.
2. [SEC-2] `/api/report` принимает и печатает сырой HTML клиента → SSRF/XSS через PDF.
3. [SEC-3] Flask запущен `debug=True` на `0.0.0.0` → удалённый дебаггер и раскрытие стека.
4. [BUG-1] `gigachat_helper` бросает `ValueError` при старте без секретов → backend не запускается в prod-режиме.
5. [PERF-1] `upload_file` читает весь CSV/Excel в память, хотя UI просит только страницу → большие файлы кладут сервер.

### Полный JSON контекст
```json
{
  "project_info": {
    "name": "Multi-LLM Analyzer",
    "type": "Full-stack web app",
    "status": "partially working",
    "purpose": "Загрузка табличных данных, очистка пропусков и получение AI‑аналитики/PDF от OpenAI, YandexGPT и GigaChat",
    "structure": "project/ -> backend/pdf_server.py (Flask API), backend/llm/* helpers, frontend/src/App.tsx + components (React), docs/*, sample CSV/XLSX, .env с ключами"
  },
  "tech_stack": {
    "backend": {
      "language": "Python 3.x",
      "framework": "Flask 2.x",
      "database": "None",
      "orm": "None",
      "async": "sync only",
      "data_processing": ["pandas", "pdfplumber", "WeasyPrint"],
      "file_storage": "temp files on disk",
      "auth": "None"
    },
    "frontend": {
      "language": "TypeScript 4.9",
      "framework": "React 18 (CRA/Webpack)",
      "state": "React useState/useEffect",
      "bundler": "react-scripts 5",
      "ui": "MUI 5",
      "visualization": "Recharts"
    },
    "integrations": ["OpenAI", "YandexGPT", "GigaChat", "WeasyPrint"]
  },
  "metrics": {
    "size": {
      "total_files": 30,
      "python_lines": 596,
      "js_lines": 2434,
      "largest_files": [
        {"file": "frontend/src/components/AnalysisResult.tsx", "lines": 1110},
        {"file": "frontend/src/App.tsx", "lines": 477},
        {"file": "backend/pdf_server.py", "lines": 329},
        {"file": "README.md", "lines": 233},
        {"file": "frontend/src/api/index.ts", "lines": 144}
      ]
    },
    "quality": {
      "overall_score": 4,
      "readability": 4,
      "structure": 3,
      "documentation": 3,
      "tests": 1,
      "security": 2,
      "performance": 4,
      "test_coverage": 0,
      "type_coverage_backend": 40,
      "type_coverage_frontend": 60,
      "lint_warnings": null,
      "security_issues": 3
    },
    "complexity": {
      "avg_function_lines": 33,
      "avg_cyclomatic_complexity": 9,
      "duplicated_code_percent": 20
    },
    "dependencies": {
      "backend_packages": 13,
      "frontend_packages": 21,
      "outdated_packages": 10,
      "vulnerable_packages": null
    }
  },
  "critical_issues": [
    {
      "id": "SEC-1",
      "type": "security",
      "severity": "critical",
      "category": "Hardcoded secrets",
      "file": ".env:2-12",
      "description": "YANDEX/OpenAI/GigaChat ключи лежат в репозитории и совпадают в backend/.env",
      "code": "OPENAI_API_KEY=sk-proj-Try...; YANDEX_API_KEY=AQVN1OxX...",
      "risk": "HIGH — компрометация аккаунтов и платных квот",
      "estimated_fix_time": "30m"
    },
    {
      "id": "SEC-2",
      "type": "security",
      "severity": "critical",
      "category": "Unsanitized HTML",
      "file": "backend/pdf_server.py:296-315",
      "description": "report_html клиента вставляется в WeasyPrint без фильтрации",
      "risk": "HIGH — SSRF/XSS через PDF генератор",
      "estimated_fix_time": "1h"
    },
    {
      "id": "BUG-1",
      "type": "bug",
      "severity": "critical",
      "category": "Startup failure",
      "file": "backend/llm/gigachat_helper.py:8-23",
      "description": "Модуль бросает ValueError при отсутствии GIGACHAT_CREDENTIALS",
      "risk": "HIGH — backend не запускается вне TEST_MODE",
      "estimated_fix_time": "45m"
    }
  },
  "high_priority_issues": [
    {
      "id": "ARCH-1",
      "type": "architecture",
      "severity": "high",
      "category": "Monolithic component",
      "file": "frontend/src/components/AnalysisResult.tsx:1-1110",
      "description": "Один компонент содержит визуализацию, экспорт, анализ и вызовы API",
      "impact": "Трудно тестировать и оптимизировать, дублирование HTML шаблонов",
      "estimated_fix_time": "4h"
    },
    {
      "id": "PERF-1",
      "type": "performance",
      "severity": "high",
      "category": "Full-file reads",
      "file": "backend/pdf_server.py:173-214",
      "description": "pandas читает весь CSV/Excel перед пагинацией",
      "impact": "Большие файлы кладут память и блокируют сервер",
      "estimated_fix_time": "3h"
    },
    {
      "id": "PERF-2",
      "type": "performance",
      "severity": "high",
      "category": "Repeated full LLM calls",
      "file": "frontend/src/App.tsx:213-244",
      "description": "Каждое добавление строк повторно отправляет весь массив в /api/analyze",
      "impact": "Латентность и квоты растут линейно от объёма данных",
      "estimated_fix_time": "3h"
    }
  ],
  "medium_priority_issues": [
    {
      "id": "QUALITY-1",
      "type": "code_quality",
      "severity": "medium",
      "category": "Loose typing/logging",
      "file": "frontend/src/components/AnalysisResult.tsx",
      "description": "Состояния объявлены как any и выводят полные данные в консоль",
      "impact": "Повышенный риск регрессий и утечек",
      "estimated_fix_time": "4h"
    },
    {
      "id": "TEST-1",
      "type": "testing",
      "severity": "medium",
      "category": "No automated tests",
      "file": "frontend/src/App.test.tsx",
      "description": "Остался шаблон CRA; тесты падают сразу",
      "impact": "Невозможно внедрить CI/CD и безопасный рефакторинг",
      "estimated_fix_time": "8h"
    }
  ],
  "low_priority_issues": [
    {
      "id": "DOC-1",
      "type": "documentation",
      "severity": "low",
      "category": "Outdated README",
      "file": "frontend/README.md",
      "description": "Фронтовый README остаётся шаблоном Create React App и не описывает продукт",
      "impact": "Новым разработчикам сложно понять сценарии запуска",
      "estimated_fix_time": "1h"
    }
  ],
  "technical_debt": {
    "level": "critical",
    "estimated_hours": 80,
    "blocks_development": true,
    "main_blockers": [
      "API ключи в репозитории — нельзя публиковать код",
      "Отсутствие тестов/CI делает любые изменения рискованными",
      "Монолитные компоненты App/AnalysisResult затрудняют расширение"
    ]
  },
  "constraints": {
    "backward_compatibility": false,
    "deadline": "none",
    "team_size": 1,
    "available_time_hours": 40,
    "environment": "local dev only",
    "expected_load": "до 1000 строк на страницу (100 МБ upload limit)"
  }
}
```

### Рекомендация
- [ ] Продолжать развивать (если >7/10)
- [x] Серьёзный рефакторинг (если 4-7/10)
- [ ] Переписать с нуля (если <4/10)

Приоритет: закрыть critical security (уборка секретов, auth/rate limiting), затем тесты/CI и декомпозиция монолитных компонентов перед новыми функциями. 
